<% card = Card.find(params[:card_id]) unless params[:card_id].nil? %>

<%= render 'home/cards', card: card %>

<%= link_to 'All Cards', cards_path %>
<%= link_to 'All Tags', tags_path %>
<%= link_to 'New Card', new_card_path %>
<%= link_to 'New Tag', new_tag_path %>
